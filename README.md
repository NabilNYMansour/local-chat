# local-chat

A small chat UI that talks to [Ollama](https://ollama.com) on your machine. Uses the Vercel AI SDK and React.

**Requirements:** Ollama running locally (default: `http://localhost:11434`).

**Run:** `pnpm dev`

**Build:** `pnpm build`
